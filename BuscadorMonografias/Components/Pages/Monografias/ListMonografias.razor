@page "/monografias"
@using BuscadorMonografias.Models

<PageTitle>Monografias</PageTitle>

<MudDataGrid T="Monografia" Items="listaMonografias" Filterable="false">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Lista de Monografías</MudText>
        <MudSpacer />
        <MudTextField T="string" ValueChanged="@(s => OnSearch(s))" Placeholder="Buscar..."
                      Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search"
                      IconSize="Size.Medium" Class="mt-0" />
    </ToolBarContent>
    <Columns>
        <PropertyColumn Property="x => x.Id" Title="ID" />
        <PropertyColumn Property="x => x.Nombre" Title="Nombre" />
    </Columns>
    <PagerContent>
        <MudDataGridPager T="Monografia" />
    </PagerContent>
</MudDataGrid>
